apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: linuxmaster
  labels:
    app: linuxmaster
    tier: db
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 10Gi
